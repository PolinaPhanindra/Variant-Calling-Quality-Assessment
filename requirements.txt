# Required dependencies for the Variant Quality Assessment tool
pytest==6.2.5      # For writing and running tests
pytest-cov==2.12.1 # For measuring test coverage
docker==5.0.3      # For interacting with Docker (optional)
pyyaml==5.4.1      # For parsing YAML configuration (if required for input/output handling)
